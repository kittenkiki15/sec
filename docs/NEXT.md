# 次の一歩

セッションをまたいでも作業を再開できるように、現在地と次にやることを書く。
**セッションの終わりに更新する**（`docs/00-development-policy.md`）。

最終更新: 2026-09-15

## 現在地

- **M0.5（リポジトリ基盤）完了。** タグ `m0.5` は `28e3a6d` を指す
- **M0（言語仕様書の確定）完了。** 仕様書の **§1〜7 が確定**し、対応するフィクスチャが
  `tests/golden/*.txt` に揃った。マイルストーンの完了条件（要件定義書 §8）を満たす
- **次は M1（字句解析・構文解析）。** 段階 1〜4 で書いたフィクスチャを緑にしていく

## M0 の記録

言語仕様書は分量が大きいため 4 段階に分け、段階ごとに Issue と PR を対応させた。

| 段階 | Issue | 内容 | 成果物 |
| --- | --- | --- | --- |
| 1 | [#3](https://github.com/kittenkiki15/sec/issues/3) | 字句・リテラル・メッセージ式の優先順位 | §1〜3 + `literals` / `messages` / `precedence` |
| 2 | [#4](https://github.com/kittenkiki15/sec/issues/4) | セル参照・範囲・ブロック・条件 | §4〜5 + `references` / `ranges` / `blocks` / `conditionals` |
| 3 | [#5](https://github.com/kittenkiki15/sec/issues/5) | 組み込みクラスとセレクタ一覧 | §6 + `errors` / `numbers` / `strings` / `booleans` / `collections` |
| 4 | [#6](https://github.com/kittenkiki15/sec/issues/6) | マクロの文法 | §7 + `macros` / `assignment` |

**確定させた判断は仕様書の付録 A にある。** ADR にしたものは `docs/adr/`（0007〜0018）。
持ち越した論点は付録 B に残してある。

**評価器がまだ無いので、フィクスチャは 1 件も実行されていない。** 走っているのは
`format.test.mjs`（形式が壊れていないこと）と `consistency.test.mjs`（期待値のエラー種別が
要件 F-8-2 にあること、すべての `*.txt` が README の割り当て表に載っていること）だけ。

## 段階 4（#6）で確定させた判断（レビュー対象）

ADR が 1 本（[0018](adr/0018-golden-macro-directive.md)）。**要件定義書は変更していない。**

- **ゴールデンテストのケースに開始記号を持たせた**（ADR-0018）。最初の行の `!macro` で
  マクロとして読む。**`^ 3` は数式なら `#Syntax`、マクロなら `3`** で、指定する手段が
  無いままでは同じ原文に矛盾する期待値が 2 つ並ぶ。**宣言部を持つ定義は `!macro` に
  起動するメッセージを添える**（`!macro from: 1 to: 3`）
- **開始記号は 3 つ。** 数式が 1 つ、マクロが 2 つ（宣言付き / 本体だけ）。
  ブックに保存する経路（F-3-1）と、その場で試す経路（受け入れ基準 3、F-6）の両方が要る
- **セレクタの理解は文脈に依存しない。** 数式とマクロの違いは構文だけで、`do:` は
  数式からも送れる（何も観測できないだけ）。§6.4 が送ってきた宿題への答え
- **代入は式ではなく文。** Smalltalk-80 とは違う。数式が拒否する範囲が構文の 1 箇所で
  決まる。`3 + (a := 1)` も `a := b := 3` も `#Syntax`
- **ブロックの中の `^` は非局所リターン。** 繰り返しから抜ける手段をこれ 1 つにした
- **`do:` と `whileTrue:` を §7 に入れた。** §5.3・§6.4・付録 B が「§7 で決める」と
  していた宿題を閉じた。**カスケード `;` は入れない**（§1.6 のトークンからも外した）
- **範囲への代入は先送り**（§7.9）。未決論点 D-4（スピル）と絡むため、
  D-4 より先に単一値だけ決めると食い違う
- **実行上限は数式にも当たる**（§7.8）。要件 N-5 と F-3-5 はマクロについて書いているが、
  `whileTrue:` は数式からも送れる。**代入が書けないことは止まる保証にならない。**
  要件の変更ではなく適用範囲の明示として仕様書に書いた（AI レビュー 1 回目の指摘）

### この段階で残した穴

- 上限の具体的な値は決めていない（§7.8）。環境で妥当な値が違うため M4 で決める
- 1 つのケースに定義できるマクロは 1 つだけ。**マクロを組み合わせる形は F-3-7 が
  入るときに考える**（ADR-0018 の「覆すとしたら」）

## M1 でやること

1. 字句解析器と構文解析器を書き、**段階 1〜4 のフィクスチャを順に緑にする**
2. **`consistency.test.mjs` のセレクタ網羅の検査を入れ替える。** 現在はキーワード
   メッセージを構文解析器なしに切り出せないため、エラー種別とファイル割り当てしか
   見ていない。構文解析器ができたら、§6 のセレクタ表とフィクスチャの突き合わせを足す
3. 構文エラーに位置（行・列）と説明文を持たせる（要件 F-8-3）。
   **位置と文言はゴールデンテストの期待値には書かない**（§0.3、§3.8）

## 積んであるもの（急がない）

- **マイルストーンのタグメッセージがタイトル 1 行だけ。** `git tag -n99` で内容を
  見たい場合は、`notes` の入力をタグメッセージにも入れる形にできる。
  ただし `notes` を空にして自動生成させる運用だと、タグメッセージには何も入らない。
  情報は Release に残るため対応は必須ではない
- 要件定義書の未決論点 D-4 / D-5 / D-7 / D-8 / D-9。必要になった段階で決める
- **M0 完了のタグ付け。** Actions の **Tag milestone** ワークフローから `m0` を作る
  （[ADR-0006](adr/0006-milestone-tags.md)）。手元から `git push origin <tag>` はしない
