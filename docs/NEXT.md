# 次の一歩

セッションをまたいでも作業を再開できるように、現在地と次にやることを書く。
**セッションの終わりに更新する**（`docs/00-development-policy.md`）。

最終更新: 2026-09-14

## 現在地

- **M0.5（リポジトリ基盤）完了。** タグ `m0.5` は `28e3a6d` を指す
- **M0（言語仕様書の確定）は段階 2 まで完了。** 仕様書 `docs/02-language-spec.md` の §1〜5 と、
  `literals` / `messages` / `precedence` / `references` / `ranges` / `blocks` / `conditionals` が入った。
  次は段階 3（#5）

## M0 の進め方

言語仕様書は分量が大きいため 4 段階に分け、段階ごとに Issue と PR を対応させる。
各段階は前段階の定義を前提にするため、**順番に進める。**

| 段階 | Issue | 内容 | 成果物 |
| --- | --- | --- | --- |
| ~~1~~ | ~~[#3](https://github.com/kittenkiki15/sec/issues/3)~~ | ~~字句・リテラル・メッセージ式の優先順位~~ **完了** | 仕様書 §1〜3 + `literals` / `messages` / `precedence` |
| ~~2~~ | ~~[#4](https://github.com/kittenkiki15/sec/issues/4)~~ | ~~セル参照・範囲・ブロック・条件~~ **完了** | 仕様書 §4〜5 + `references` / `ranges` / `blocks` / `conditionals` |
| 3 | [#5](https://github.com/kittenkiki15/sec/issues/5) | 組み込みクラスとセレクタ一覧 | 仕様書 §6 + `numbers` / `strings` / `collections` |
| 4 | [#6](https://github.com/kittenkiki15/sec/issues/6) | マクロの文法 | 仕様書 §7 + `macros` / `assignment` |

フィクスチャは `tests/golden/*.txt`。

### この段階で注意すること

- **評価器がまだ無いので、追加したゴールデンテストは 1 件も通らない。** それが M1 以降の
  作業リストになる。フィクスチャは現状ただのデータファイルで、何も実行されていない
- ~~段階 1 で、全フィクスチャが `parseGoldenFile` で解析できることを検証するテストを追加する~~
  — `tests/golden/format.test.mjs` として追加済み。緑
- 段階 3 で **D-6（空セルの値）を決める必要がある。** 未決のまま `sum` の仕様は書けない。
  §4.3 も範囲の中身を扱うセレクタを §6 送りにしている
- 段階 3 で **複数のエラーが同時に生じたときどれが伝播するかを決める必要がある。**
  §3.7 は「上流のエラーは下流へ伝播する」としか述べておらず、`(3 foo) + (1 / 0)` のように
  受け手と引数の双方がエラーになる場合を定めていない。**これが決まるまで §3.6 の評価順序を
  ゴールデンテストで固定できない**（純粋な数式では、エラーの伝播以外に評価順序を観測する
  手段が無いため）。段階 1 の棚卸しでケースを書こうとして見つかった
- 段階 4 では、数式が副作用の構文を拒否することを**文法の側から**改めて扱う。
  段階 1 では `messages.txt`、段階 2 では `blocks.txt` に「数式が拒否する」ケースだけを
  入れてある（§3.1、§5.1）。マクロのブロックが文の列と一時変数を持てることは §7 で定める

### 段階 2 で確定させた判断（レビュー対象）

仕様書の付録 A に一覧がある。特に次の 3 つは覆すなら ADR が要る。

- **`A1 = A2` は値の比較。** `Cell` は `=` を定義せず、ADR-0008 の委譲規則に任せる
- **範囲の表記は正準形と同じ `A1 to: B10`。** 糖衣の設定で読み方が揺れないようにするため
- **逆向きの範囲は正規化する。** `B10 to: A1` は `A1 to: B10` と同じ矩形

**数式のブロックの本体は式ちょうど 1 つ**とした（F-2-10）。文の列と一時変数を持つ
ブロックは段階 4（#6）でマクロの文法として扱う。

### 段階 1 で確定させた判断（レビュー対象）

仕様書 `docs/02-language-spec.md` の付録 A に一覧がある。特に次の 3 つは覆すなら ADR が要る。

- **指数の `e` は小文字だけ。** `1E3` は `E3` がセル参照の形と衝突するため `#Syntax`
- **`5.` は小数として認めない。** マクロの文区切り `.` と紛れるため
- **負のリテラルと二項の `-` は直前のトークンで区別する。** `3 -4` は `-1`

段階 3 以降で使うセレクタのうち、段階 1〜2 のフィクスチャが先に使ってしまったものがある
（`abs` / `negated` / `squared` / `max:` / `min:` / `between:and:` / `size` / `/` / `>`）。
段階 3（#5）のセレクタ一覧には必ずこれらを含めること。

## M0 の後

M1（字句解析・構文解析）。段階 1〜2 で書いたフィクスチャを緑にするところから始める。

## 積んであるもの（急がない）

- **マイルストーンのタグメッセージがタイトル 1 行だけ。** `git tag -n99` で内容を
  見たい場合は、`notes` の入力をタグメッセージにも入れる形にできる。
  ただし `notes` を空にして自動生成させる運用だと、タグメッセージには何も入らない。
  情報は Release に残るため対応は必須ではない
- 要件定義書の未決論点 D-4 / D-5 / D-7 / D-8 / D-9。必要になった段階で決める
