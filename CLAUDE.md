# CLAUDE.md

このリポジトリで作業するときの規約。Claude Code はセッションの最初にこれを読む。

## プロジェクト

**sec** — Smalltalk 風の言語で数式やマクロを記述できる、Excel 風スプレッドシートの Web アプリ。
バックエンドを持たず、ブラウザだけで動く。Claude Code から MCP 経由でシートを操作できるようにする。

- 要件定義: `docs/01-requirements.md`
- 開発方針: `docs/00-development-policy.md`
- 設計判断の記録: `docs/adr/`

**仕様に関わる判断は必ず ADR を確認してから行う。** 決定済みの論点を蒸し返さないこと。

## コマンド

| 目的 | コマンド |
| --- | --- |
| 依存関係のインストール | `pnpm install` |
| 型検査 | `pnpm typecheck` |
| Lint + フォーマット検査 | `pnpm lint` |
| 自動修正 | `pnpm lint:fix` |
| テスト | `pnpm test` |
| テスト（監視） | `pnpm test:watch` |
| カバレッジ | `pnpm coverage` |
| **まとめて実行（コミット前に必ず）** | `pnpm check` |

Node 22 以上、pnpm 10。パッケージを追加したら `package.json` の `typecheck` スクリプトに
その `tsconfig.json` を足すこと。

## アーキテクチャ

```
packages/core   計算コア（字句解析・構文解析・評価器・シートモデル・依存グラフ・再計算）
packages/cli    core を叩く REPL とバッチ評価。開発時のフィードバックループの基盤
packages/web    グリッド UI、数式バー、マクロエディタ
packages/mcp    MCP サーバー
```

現時点で存在するのは `core` のみ。他はマイルストーンに従って追加する。

### 破ってはいけない規約

1. **依存方向は `core ← cli / web / mcp` の一方向のみ。** `core` は他のパッケージを import しない。
2. **`core` はブラウザ API にも Node API にも依存しない。**
   `packages/core/tsconfig.json` は `lib: ["ES2023"]`, `types: []` で、DOM も `@types/node` も
   読み込んでいない。`window` や `process` を書くと型エラーになる。これは意図的な防壁なので緩めない。
3. **数式・マクロの評価に `eval` / `new Function` を使わない。** 必ず自前の評価器で評価する（要件 N-4）。
4. **評価器のループと再帰には必ず上限を掛ける。** ステップ数・時間・再帰深度（要件 N-5）。
5. **数式の評価経路に `Math.random()` / `Date.now()` / `new Date()` を直接書かない。**
   決定性が要件（F-4-4）。時刻や乱数は明示された API 経由でのみ扱う。
6. **数式は副作用を持てない。** セルへの書き込みやマクロ呼び出しができる経路を数式側に作らない（F-2-10）。

## テスト駆動開発（必須）

**このプロジェクトはテスト駆動開発で進める。** 根拠と例外は
[ADR-0004](docs/adr/0004-tdd.md)。

### サイクル

1. **Red** — 失敗するテストを書く。`pnpm test` を実行して**落ちることを必ず確認する。**
   落ちないテストは仕様を検証している保証がない。
2. **Green** — テストを通す最小限の実装を書く。先回りして機能を足さない。
3. **Refactor** — テストが緑のまま整理する。

### 守ること

- **実装を依頼されたら、まずテストを書き、落ちることを示してから実装に進む。**
  「テストを書いた」「落ちることを確認した（このエラーで落ちた）」を明示的に報告する。
  確認せずに実装へ進まない。
- **バグ修正も同じ。** 先にそのバグを再現する失敗テストを書く。
  再現しないテストを書いて修正しても、直ったことの証明にならない。
- **期待値を実装の出力に合わせて書き換えない。** 実装が期待値に合わないなら、
  疑うのはまず実装。期待値の方が誤っていると判断したら、変更前に理由を述べて確認を取る。
- **テストを先のコミットに置く。** 差分だけでは順序が分からないため、履歴が唯一の証拠になる。
  1 コミットにまとめる場合はコミット本文にその旨を書く。
- テストは振る舞いに対して書く。内部実装の詳細に結びつけない。
- 境界値とエラーケースを必ず含める（ゼロ除算、型不一致、空、上限超過）。

### 例外

次に限りテストを先に書かなくてよい。**これ以外に広げない。**

| 例外 | 条件 |
| --- | --- |
| 探索的スパイク | 捨てる前提の試作。**本実装は必ず TDD で書き直す** |
| 性能ベンチマーク | 計測が目的で合否判定が無いもの |
| UI の見た目の調整 | 振る舞い（状態遷移・入出力）を変えないもの |
| 機械的なリファクタリング | 既存テストが振る舞いを担保し、それが緑のまま通ること |
| 設定ファイル・ドキュメント | — |

例外を適用したときは、その理由を報告する。黙って適用しない。

**CI やツールのスクリプト（`.github/scripts/` 配下）もテスト対象。** 「設定ファイル」の例外に
含めない。振る舞いを持つコードである以上、扱いを変える理由がない。
テストは `.github/scripts/**/*.test.mjs` に置く（`vitest.config.ts` の対象に含めてある）。

## ゴールデンテスト

`tests/golden/*.txt` が言語仕様の実体。ハーネスは `packages/core/src/testing/golden.ts`。

```text
"行頭が二重引用符の行はコメント

3 + 4 * 2
=> 14

"式も期待値も複数行に書ける
#(1 2 3)
  inject: 0
  into: [:a :b | a + b]
=> 6
```

- 空行がケースの区切り。ケース内で最初の `=>` 行から後ろが期待値。
- **マクロのケースは最初の行に `!macro` を置く**（[ADR-0018](docs/adr/0018-golden-macro-directive.md)）。
  数式とマクロは開始記号が違い、同じ原文でも結果が変わる（`^ 3` は数式なら `#Syntax`）。
  無いケースは数式として読む。**宣言部を持つ定義は `!macro from: 1 to: 3` のように
  起動するメッセージを添える**（添えなければ宣言部の無い本体として実行する）。
- **セルに値を入れて評価するには、式より前に `!A1 := 1` の形で書く**（[ADR-0009](docs/adr/0009-golden-sheet-directives.md)）。
  区切りはセルへの代入と同じ `:=`（ADR-0007 D-3）。最初の `:=` だけが区切りなので、
  右側に数式（`!B1 := =A1 + 1`）も書ける。指定はケースごとに独立。
- **構文や組み込みセレクタを追加・変更したら、必ず対応するゴールデンテストを追加・更新する。**
- **実装を通すために期待値を書き換えない。** 期待値の変更は仕様変更であり、ADR が要る。
- 言語仕様の実装も上の TDD サイクルに従う。順序は「ゴールデンテストを書く →
  落ちることを確認 → 実装する」。ゴールデンテストはこのプロジェクトにおける Red の主役。

## 作業の進め方

- **1 セッション 1 タスク。** 終わったらコミットする。
- **着手前に計画を出す。** 実装が始まってからの方針転換が一番高くつく。
- **計画の最初の一歩は必ずテスト。** 実装から始まる計画は TDD 違反なので書き直す。
- **1 PR は 400 行以内を目安に。** ゴールデンテストの追加行はこの勘定に入れない。
- **`pnpm check` が緑になってからコミットする。**
- **2 回目に必要になるまで抽象化しない。** 使われていない抽象を先回りで作らない。
- 判断に迷う仕様上の論点が出たら、勝手に決めずに `docs/adr/` を確認し、
  未決なら実装を止めて利用者に聞く。

## マイルストーンのタグ付け

マイルストーン完了時は Actions の **Tag milestone** ワークフローからタグと Release を作る
（[ADR-0006](docs/adr/0006-milestone-tags.md)）。タグ名は `m0.5` / `m1` / `m3.5` の形式。

**手元から `git push origin <tag>` はしない。** セッションによってはタグ ref の push が
egress ポリシーで拒否される。ワークフローはこの制約を受けない。

## Issue と PR

まとまった作業は Issue を立ててから着手し、**PR の本文に `Closes #<番号>` を書いて
紐づける。** マージ時に Issue が自動で閉じ、後から「この変更は何のためだったか」を
Issue から辿れる。

現在地と次にやることは `docs/NEXT.md` にある。**セッションの終わりに更新する。**

## コミット

- 1 行目は `<type>: <日本語の要約>`。type は `feat` / `fix` / `docs` / `test` / `refactor` / `chore` / `ci`。
- 本文には「何を」ではなく「なぜ」を書く。差分を読めば分かることは書かない。
- コミットにモデル名を書かない。

## コードスタイル

Biome が整形と Lint を担う。手で体裁を直さず `pnpm lint:fix` に任せる。

- コメントは日本語。**何をしているかではなく、なぜそうしたかを書く。**
- 公開 API の名前は Smalltalk の慣習に合わせる（セレクタは camelCase、キーワードメッセージは `at:put:` 形式）。
- `any` と非 null アサーション `!` は Lint で禁止。`noUncheckedIndexedAccess` が有効なので、
  配列アクセスの結果は `undefined` の可能性を潰してから使う。
